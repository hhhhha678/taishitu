# Frontend Directory Note

The currently served dashboard page is in `../static`.

The backend mounts `static/index.html`, `static/app.js`, and `static/styles.css` through `backend/app/main.py`. This `frontend` directory is an earlier Vite/React implementation kept as a reference/backup and is not used by the current running page.
